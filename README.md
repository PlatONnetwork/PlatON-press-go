# platon-press-go

### build 

```shell script
cd cmd/batch
go build
```

### run batch transfer

```shell script
./batch -chain_id <chain_id> -url <url> -accounts <accounts.json> -count <number>
```

